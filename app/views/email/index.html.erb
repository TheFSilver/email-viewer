<section class="content">
  <div class="container">
    <div class="row viewer">
      <div class="col-3 liste">
        <div class="list-group">
          <% @email.each do |email| %>
            <%= link_to "#{email.object}", email_path(email, email_id: email.id), class: 'show list-group-item list-group-item-action', remote: true %>
          <% end %>
        </div>
      </div>
      <div class="col-9 ">
        <% @email.each do |email| %>
<<<<<<< HEAD
          <div class="container contenu">
=======
        <div class="container contenu">
          <%= link_to " Delete", "#", class: "btn btn-sm btn-danger bouton far fa-trash-alt", remote: true %>
>>>>>>> master
          <h3><%= email.object %></h3>
          <hr>
          <p><%= email.body %></p>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</section>
